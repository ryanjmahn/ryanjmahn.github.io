# Credits

## Portrait
`assets/src/ryan.png` — personal photo, converted offline by `tools/ascii.py`
into a static monospace ascii-art `<pre>`, each character colored to match
its sampled pixel. The source photo is not loaded by the page; only the
converted markup is pasted into `index.html`.
